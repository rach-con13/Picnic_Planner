import styled from "styled-components";
import { Container } from "../../Styles/Elements/Container/Container";

export const HeaderSection = styled(Container).attrs({
  as: "header",
})`
  width: 100%;
`;
